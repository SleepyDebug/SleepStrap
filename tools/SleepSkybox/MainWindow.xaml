<Window x:Class="SleepSkybox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SleepSky"
        Width="760"
        Height="580"
        MinWidth="680"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Background="#FF000000"
        Foreground="White"
        AllowDrop="True"
        DragOver="Window_DragOver"
        Drop="Window_Drop">
    <Grid ClipToBounds="True">
        <Rectangle IsHitTestVisible="False" Opacity="0.32">
            <Rectangle.Fill>
                <DrawingBrush TileMode="Tile" Viewport="0,0,230,170" ViewportUnits="Absolute">
                    <DrawingBrush.Drawing>
                        <DrawingGroup>
                            <GeometryDrawing Brush="#00FFFFFF" Geometry="M0,0 H230 V170 H0 Z" />
                            <GeometryDrawing Brush="#A8FFFFFF"><GeometryDrawing.Geometry><EllipseGeometry Center="24,29" RadiusX="1.1" RadiusY="1.1" /></GeometryDrawing.Geometry></GeometryDrawing>
                            <GeometryDrawing Brush="#5CFFFFFF"><GeometryDrawing.Geometry><EllipseGeometry Center="89,18" RadiusX="0.7" RadiusY="0.7" /></GeometryDrawing.Geometry></GeometryDrawing>
                            <GeometryDrawing Brush="#78FFFFFF"><GeometryDrawing.Geometry><EllipseGeometry Center="171,42" RadiusX="1.25" RadiusY="1.25" /></GeometryDrawing.Geometry></GeometryDrawing>
                            <GeometryDrawing Brush="#70FFFFFF"><GeometryDrawing.Geometry><EllipseGeometry Center="211,96" RadiusX="0.85" RadiusY="0.85" /></GeometryDrawing.Geometry></GeometryDrawing>
                            <GeometryDrawing Brush="#46FFFFFF"><GeometryDrawing.Geometry><EllipseGeometry Center="54,132" RadiusX="0.7" RadiusY="0.7" /></GeometryDrawing.Geometry></GeometryDrawing>
                            <GeometryDrawing Brush="#66FFFFFF"><GeometryDrawing.Geometry><EllipseGeometry Center="132,151" RadiusX="1" RadiusY="1" /></GeometryDrawing.Geometry></GeometryDrawing>
                        </DrawingGroup>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Border Margin="20" Padding="30" CornerRadius="18" Background="{StaticResource PanelBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="SleepSky" FontSize="28" FontWeight="SemiBold" />
                        <TextBlock Text="SleepBlox tools" Margin="1,4,0,0" FontSize="12"
                                   Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                    <Border Grid.Column="1" Padding="10,5" VerticalAlignment="Top" Background="#FF25252C" CornerRadius="8">
                        <TextBlock Text="PNG → ROBLOX SKY" FontSize="10" FontWeight="SemiBold" />
                    </Border>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,32,0,18">
                    <TextBlock Text="Turn an equirectangular PNG panorama into the six Roblox sky files."
                               FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock Text="Your original image and Roblox installation are never changed."
                               Margin="0,6,0,0" Foreground="{StaticResource TextMutedBrush}" />

                    <Border Margin="0,26,0,0" Padding="20" Background="{StaticResource PanelRaisedBrush}" CornerRadius="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="PNG panorama" FontWeight="SemiBold" />
                                <TextBlock x:Name="InputPathText" Margin="0,5,0,0"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Text="Drop a 2:1 PNG here, or choose one." TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <Button Grid.Column="1" Margin="16,0,0,0" Content="Choose PNG" Click="ChoosePng_Click" />
                        </Grid>
                    </Border>

                    <Border Margin="0,10,0,0" Padding="20" Background="{StaticResource PanelRaisedBrush}" CornerRadius="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Output folder" FontWeight="SemiBold" />
                                <TextBlock x:Name="OutputPathText" Margin="0,5,0,0"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Text="A new folder will be created beside the PNG." TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <Button Grid.Column="1" Margin="16,0,0,0" Content="Change" Click="ChooseOutput_Click"
                                    Style="{StaticResource DarkButton}" />
                        </Grid>
                    </Border>

                    <TextBlock x:Name="StatusText" Margin="3,18,3,0" Foreground="{StaticResource TextMutedBrush}"
                               Text="Ready." TextWrapping="Wrap" />
                </StackPanel>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Creates PlatformContent\pc\textures\sky" VerticalAlignment="Center"
                               Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                    <Button x:Name="OpenOutputButton" Grid.Column="1" Margin="12,0,0,0" Content="Open folder"
                            Style="{StaticResource DarkButton}" IsEnabled="False" Click="OpenOutput_Click" />
                    <Button x:Name="ConvertButton" Grid.Column="2" Margin="10,0,0,0" Content="Create skybox"
                            IsEnabled="False" Click="Convert_Click" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
