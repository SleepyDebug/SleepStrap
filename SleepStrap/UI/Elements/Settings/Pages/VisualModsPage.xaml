<ui:UiPage x:Class="SleepStrap.UI.Elements.Settings.Pages.VisualModsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:dmodels="clr-namespace:SleepStrap.UI.ViewModels.Settings"
      mc:Ignorable="d" d:DataContext="{d:DesignInstance dmodels:VisualModsViewModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="760" d:DesignWidth="800" Title="Skybox &amp; Textures" Scrollable="True">
    <StackPanel Margin="0,0,14,20">
        <Border Padding="22" CornerRadius="16" BorderThickness="1" BorderBrush="#66FFFFFF">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#4418181A" Offset="0" />
                    <GradientStop Color="#66232326" Offset="0.55" />
                    <GradientStop Color="#33101012" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="YOUR ROBLOX, AFTER DARK" FontSize="12" FontWeight="SemiBold" Foreground="White" />
                    <TextBlock Text="Skybox &amp; Textures" Margin="0,5,0,0" FontSize="29" FontWeight="SemiBold" />
                    <TextBlock Text="Import a panorama or swap the entire material set in one place." Margin="0,8,0,0" MaxWidth="540" FontSize="13" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <ui:SymbolIcon Grid.Column="1" Symbol="WeatherMoon24" FontSize="54" Foreground="White" Margin="24,0,4,0" />
            </Grid>
        </Border>

        <ui:InfoBar Margin="0,14,0,0" IsOpen="True" IsClosable="False" Severity="Warning" Title="Unofficial client asset modifications" Message="Visual mods replace local Roblox texture files. SleepStrap never injects code, but no third-party launcher can promise zero account-enforcement risk." />

        <TextBlock Text="CUSTOM SKYBOX" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SleepStrapAccentBrush}" Margin="2,22,0,8" />
        <Border Padding="18" CornerRadius="12" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,18,0">
                    <TextBlock Text="Import one panoramic sky image" FontSize="17" FontWeight="Medium" />
                    <TextBlock Text="SleepStrap maps a 2:1 equirectangular image into six 512px cube faces, compresses them to Roblox-compatible BC1 textures, and installs them automatically." Margin="0,6,0,0" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding SkyboxStatus}" Margin="0,10,0,0" FontSize="12" FontWeight="SemiBold" Foreground="White" />
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <ui:Button Content="Import panorama" Icon="ImageAdd20" Appearance="Primary" Command="{Binding ImportSkyboxCommand}" MinWidth="150" />
                    <ui:Button Content="Restore previous sky" Icon="ArrowReset20" Command="{Binding RemoveSkyboxCommand}" Visibility="{Binding RemoveSkyboxVisibility}" Margin="0,8,0,0" MinWidth="150" />
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Text="MATERIAL TEXTURES" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SleepStrapAccentBrush}" Margin="2,22,0,8" />
        <Border Padding="18" CornerRadius="12" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,18,0">
                    <TextBlock Text="Dark texture pack" FontSize="17" FontWeight="Medium" />
                    <TextBlock Text="Switches Roblox materials, terrain, water and supporting texture files to the bundled dark set. Turn it off to restore every file that was there before." Margin="0,6,0,0" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="Your existing texture mods are backed up before anything is changed." Margin="0,10,0,0" FontSize="12" Foreground="White" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Basic" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ui:ToggleSwitch IsChecked="{Binding DarkTexturesEnabled, Mode=TwoWay}" />
                    <TextBlock Text="Dark" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold" Foreground="White" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Margin="0,16,0,0" Padding="14,11" CornerRadius="10" Background="#22000000" BorderBrush="#44FFFFFF" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:Button Grid.Column="1" Content="Open Modifications folder" Icon="Folder20" Command="{Binding OpenModsFolderCommand}" />
            </Grid>
        </Border>
    </StackPanel>
</ui:UiPage>
