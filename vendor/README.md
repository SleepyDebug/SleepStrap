# Vendored dependencies

`Wpf.Ui/` contains the WPF UI dependency used to build SleepStrap. Keeping third-party code under `vendor/` separates it from the SleepStrap application source.

The dependency keeps its own project names, namespaces, and license notices.
